<h1><%= @post.title %></h1>

<div>
<%= @post.body %>
</div>
<br>
<%= link_to "Edit Post", edit_post_path(@post) %> &nbsp
<%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure? Item will be permanantly deleted"} %> &nbsp
<%= link_to "Post Listing", root_path(@post) %>
